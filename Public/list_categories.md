### GET /category/
* Description: List all categories only by name 
* Response: 
    * 200: `['category', ...]` 
* Example cURL:
  ```
    curl --request GET \
    --url https://requests.onlineapps.cloud/category
